<%= simple_form_for([:user, @workhour]) do |f| %>
  <div class="modal-body">
  <% if @workhour.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workhour.errors.count, "error") %> prohibited this workhour from being saved:</h2>

      <ul>
      <% @workhour.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="input-append date datepicker"> 	 	 
	  <%= f.text_field :date_of_workhour, :class=>'datepicker', :placeholder=>'Select date', id: "prependedInput" %> 
    <span class="add-on"><i class="fa fa-table"></i></span>	 
  </div>
  

  
  <div class="field">
    <%= f.label :hours %><br />
    <%= f.text_field :hours %>
  </div>
  <div class="field">
    <%= f.label :factor %><br />
    <%= f.number_field :factor %>
  </div>


  </div>
  <div class="modal-footer">
  <div class="actions">
    <%= f.submit %>
  </div></div>
<% end %>

<script>
	create_datepicker();
</script>
