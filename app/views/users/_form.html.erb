<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hourly_gross %><br />
    <%= f.text_field :hourly_gross %>
  </div>
  <div class="field">
    <%= f.label :hourly_net_wage %><br />
    <%= f.text_field :hourly_net_wage %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
